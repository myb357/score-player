# ============================================================================
# 同步任务 cron 表（供容器内 supercronic 使用）
# 默认：每小时的第 15 分钟执行一次全量/增量同步。
# 可按需修改频率，例如每 30 分钟：*/30 * * * *
# 时区由容器 TZ 环境变量控制（默认 Asia/Shanghai）。
# ============================================================================
15 * * * * /app/sync_all.sh
